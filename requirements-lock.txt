# Exact dependency versions this project is verified against.
#
# This is a pip *constraints* file, NOT a replacement for pyproject.toml. The
# abstract lower-bound declarations in pyproject.toml stay as-is so a local
# `pip install -e .` keeps resolving flexibly for contributors. This file pins
# the exact resolved versions (direct + transitive) so CI builds are
# reproducible instead of drifting as new releases land upstream.
#
# Usage:  pip install -e ".[dev]" -c requirements-lock.txt
#
# Verified interpreter: CPython 3.13 (the project's primary/target runtime).
# numpy 2.5.x requires Python >= 3.11, so these pins are not installable on the
# 3.10 floor; CI applies this file only on its 3.13 job and leaves the 3.10 job
# unpinned as a lower-bound compatibility check.
#
# Regenerate after intentionally bumping a dependency: from a clean virtualenv,
#   pip install -e ".[dev]"
#   pip freeze --exclude-editable > requirements-lock.txt
# then re-add this header and commit.

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.14.1
certifi==2026.6.17
click==8.4.2
colorama==0.4.6
fastapi==0.139.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
numpy==2.5.1
opencv-python==5.0.0.93
pydantic==2.13.4
pydantic_core==2.46.4
PyJWT==2.13.0
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.51.0
